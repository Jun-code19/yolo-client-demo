# 核心代码目录（与 edge-ai-box 并列的 yolo-demo）
YOLO_CORE=../yolo-demo
# 前端 Docker 构建 context（包含 yolo-demo + edge-ai-box 的父目录）
YOLO_CLIENT_DEMO=..

# 容器内 detect / data 服务
DEPLOY_MODE=edge
DATABASE_URL=postgresql://postgres:admin123@postgres:5432/yolo
